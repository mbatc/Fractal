#include "graphics\OpenGL\flGLWindowRenderTarget.h"

using namespace flEngine;
using namespace flEngine::Graphics;

#if flUSING(flPLATFORM_WINDOWS)

// GLWindowRenderTarget* GLWindowRenderTarget::Create(Platform::Window *pWindow, flIN PixelFormat colourFormat, flIN PixelComponentType pixelComponentType, flIN DepthFormat depthFormat)
// {
//   return flNew GLWindowRenderTarget(pWindow, colourFormat, pixelComponentType, depthFormat);
// }

#endif
